import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pfe.io.com',
  appName: 'pfe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
